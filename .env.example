# このファイルをコピーして .env を作成し、値を埋めてください(ローカル実行用)。
# 本番(GitHub Actions)ではリポジトリの Secrets に同名で登録します。

GEMINI_API_KEY=
# 将来のモデル廃止・変更に備え、コード側では固定せず環境変数で指定する
GEMINI_MODEL=gemini-2.5-flash

RSS_URL=

SUPABASE_URL=
SUPABASE_KEY=

# 何時間おきに投稿処理を行うか(既定48時間 = 2日に1回)
RUN_INTERVAL_HOURS=48
